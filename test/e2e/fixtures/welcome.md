# E2E Welcome Fixture

This file is opened as a tab by the Playwright E2E suite to give the editor a
deterministic, committed document to operate on (instead of a developer's local
files, the way the old CDP scripts did).

This is a paragraph long enough to be a reliable click/selection target for the
block-conversion and selection-toolbar tests.

- list item one
- list item two

| col a | col b |
| ----- | ----- |
| 1     | 2     |
