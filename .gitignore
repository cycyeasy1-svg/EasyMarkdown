node_modules/
out/
dist/
dist-update-demo/
dist-update-demo-handoff/
dist-update-demo-handoff.zip
dist-update-demo-handoff.zip.sha256
*.log
.DS_Store

# mobile (Capacitor) build output + native build artifacts
dist-mobile/
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/DerivedData/
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/app/src/main/assets/public/
.capacitor/

# Android release signing — NEVER commit these (lose the keystore = can't update the app)
android/key.properties
*.keystore
*.jks

# local test fixtures (generated)
test-articles/

# Playwright E2E output (committed fixtures under test/e2e/fixtures ARE tracked)
test-results/
playwright-report/
.playwright/

# scratch/preview images and design references
build/_*.png
*参考*.html

# generated at dist time from onboarding.js (scripts/gen-readme.mjs)
build/README.dist.md

# device build (xcodebuild -derivedDataPath)
ios/App/build-device/

# release-pack local config (zip password — never commit)
scripts/pack.config.local.json
